/* 包含相关的头文件 */#include <stdio.h>#include <stdlib.h>#include <netinet/in.h>#include <arpa/inet.h>#include <unistd.h>#include <fcntl.h>#include <sys/stat.h>#include <sys/types.h>#include <sys/socket.h>#include <strings.h>/* 定义表示端口号的常量 */#define SERV_PORT 4568/* 定义表示表IP的常量 */#define SERV_IP "127.0.0.1"// UDP clientint main(int argc, char *argv[]) {    /* 定义相关的变量 */    int server_sockfd, len;    struct sockaddr_in serv_addr;    int sin_size;    /* 定义一个数组发送与接收数据 */    char my_buffer[256];    int i = 0;    /* 建立一个 socket */    if ((server_sockfd = socket(AF_INET, SOCK_DGRAM, 0)) < 0) {        /* 输出错误 */        perror("error");        exit(1);    } else {        /* 输出提示信息 */        printf("socket created.\n");        printf("socked id: %d \n", server_sockfd);        printf("remote ip: %s \n", SERV_IP);        printf("remote port: %d \n", SERV_PORT);    }    /* 长度 */    // len = sizeof(struct sockaddr_in);    /* 空间地址结构体所在的内存空间 */    bzero(&serv_addr, sizeof(serv_addr));    /* 填充地址与端口的信息 */    serv_addr.sin_family = AF_INET;    /* 端口 */    serv_addr.sin_port = htons(SERV_PORT);    serv_addr.sin_addr.s_addr = inet_addr(SERV_IP);    /* 循环 */    while (1) {        /*清空my_buffer所在的内存 */        bzero(my_buffer, sizeof(my_buffer));        /* 设置地址结构长度 */        sin_size = sizeof(struct sockaddr_in);        /* 接收信息 */        len = read(STDIN_FILENO, my_buffer, sizeof(my_buffer));        /* 发送信息 */        sendto(server_sockfd, my_buffer, len, 0, (struct sockaddr *) &serv_addr, sin_size);        /* 输出结果 */        printf("Input message: %s \n", my_buffer);        /* 这是接收到的信息 */        len = recvfrom(server_sockfd, my_buffer, sizeof(my_buffer), 0, (struct sockaddr *) &serv_addr, &sin_size);        /* 输出计数 */        printf("%d:", i);        /* 计数自加 */        i++;        /* 这是服务器返回的信息 */        printf("Received message: %s \n", my_buffer);    }} 